# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Tool caches
.ruff_cache/
.pytest_cache/

# Virtual environments
.venv

# Runtime usage counter -- ephemeral on Streamlit Cloud, not for VCS.
crcglot_stats.json

# Streamlit secrets (Upstash credentials etc.).  NEVER commit.
.streamlit/secrets.toml

# Claude Code per-user settings (machine-local, do not commit).
.claude/settings.local.json

# Local Claude project notes -- documents the build process / CI plumbing
# for future Claude sessions on this repo.  Not for public consumption.
CLAUDE.md
