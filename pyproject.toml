[project]
name = "st-crcglot"
version = "0.2.0"
description = "Streamlit UI for crcglot — multi-language CRC code generator."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "crcglot>=0.7.0,<0.8",
    "streamlit>=1.57.0",
]
